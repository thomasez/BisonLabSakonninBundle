<form id="mtDeleteForm" method="post" action="{{ path('messagetype_delete', {'id': entity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this Message Type?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entity.id) }}">
    <input type="submit" value="Delete Message Type">
</form>
