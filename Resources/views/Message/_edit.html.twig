{# Gonna be added to by the grab js function
{{ form_start(edit_form, { 'attr': {'id':'editSakonninMessageForm', 'onSubmit': 'return submitEditSakonninMessage();'}}) }}
 #}
{{ form_start(edit_form, { 'attr': {'id':'editSakonninMessageForm'}}) }}
    {{ form_widget(edit_form) }}
    <button type="submit" class="btn btn-primary">Save</button>
{{ form_end(edit_form) }}
